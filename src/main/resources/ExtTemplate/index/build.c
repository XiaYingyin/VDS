#include "exti.h"

void initIndexMeta(Relation index)
{
	
}

/*
 *	build() -- build index structure.
 */
IndexBuildResult *buildIndexStructure(Relation heap, Relation index, IndexInfo *indexInfo)
{
	IndexBuildResult *result;

    /*
        write your codes
    */

	return result;
}